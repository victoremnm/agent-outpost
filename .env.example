# Copy to .env and fill in your own node's details. .env is gitignored --
# never commit real addresses/hostnames here, especially once this repo
# is public.

HOMELAB_TAILSCALE_IP=100.x.x.x
HOMELAB_SSH_USER=youruser
HOMELAB_HOSTNAME=your-hostname
